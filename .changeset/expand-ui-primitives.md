---
"@mivabyte/ui": minor
---

Add the Container primitive and expanded typography, action, overlay, card, and tab APIs. Correct AspectRatio sizing, expose its props, align the Secondary button hover state with the rest of the button variants, and include the shared token stylesheet in package builds.
