---
"@mivabyte/ui": minor
---

Add the Container primitive and expanded typography, action, overlay, card, and tab APIs. Correct AspectRatio sizing, expose its props, and include the shared token stylesheet in package builds.
